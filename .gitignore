# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
out/

# Node (프론트엔드는 Docker 안에서 빌드하지만 로컬 작업 시 생긴다)
node_modules/
frontend/dist/

# CD 러너가 만드는 산출물 (Dockerfile 의 prebuilt 스테이지가 읽는다).
# 커밋 대상이 아니지만 .dockerignore 에서는 반드시 통과시켜야 한다.
/dist/

# OS
.DS_Store

# Env
.env

# k8s Secret (secret.env.example 을 복사해서 만든다)
k8s/base/secret.env

# Logs
*.log

# 캡처 도구가 실제 페이지에서 떠온 픽스처. 검토 후 손으로 옮겨 커밋한다.
# 원본에는 판매자 개인정보가 섞일 수 있으므로 자동 커밋되지 않게 막는다.
backend/app-checker/src/test/resources/fixtures/captured/
